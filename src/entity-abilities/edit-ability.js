import * as Cesium from "cesium";
import { AbilityEntity } from "../ability-entity.js";

export class EditAbility {
  /**
   * @param {AbilityEntity.DrawnEntity} drawnEntity
   */
  static edit(drawnEntity) {
    const points = drawnEntity.points;
    const viewer = drawnEntity.viewer;
  }
}
